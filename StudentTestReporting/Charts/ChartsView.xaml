<UserControl x:Class="StudentTestReporting.Charts.ChartsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:c="clr-namespace:StudentTestReporting.Charts"
             xmlns:local="clr-namespace:StudentTestReporting"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <!--<UserControl.DataContext>
        <vm:TestViewModel />
    </UserControl.DataContext>-->
    <Grid>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Increasing test subject: "/>
            <TextBlock Text="{Binding Test.Subject}" />
        </StackPanel>
    </Grid>
</UserControl>
